[phases.setup]
nixPkgs = ["php82", "php82Extensions.pgsql", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.pdo"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT --public public"